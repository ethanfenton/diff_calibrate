% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/burden.R
\name{calculate_burden}
\alias{calculate_burden}
\title{Calculate sample-size-adjusted DE burden across cell types}
\usage{
calculate_burden(
  data,
  de_fun,
  model_type = c("auto", "spline", "linear"),
  pooling = c("auto", "pooled", "shared_shape", "stratified"),
  adaptive_alpha = TRUE,
  alpha = 0.05,
  reference_n = NULL,
  min_concordance = NULL,
  concordance_metric = "cat_overlap",
  weight_floor = 0.1,
  sig_level = 0.05,
  grid_args = list(),
  k = 5,
  k_permute = 5,
  seed = 1,
  ...
)
}
\arguments{
\item{data}{Named list, one element per cell type, each
\verb{list(counts = <genes x cells matrix>, group = <two-level vector>)}.
Names give cell type labels.}

\item{de_fun}{DE backend: \code{"wilcoxon"}, \code{"edger"}, \code{"deseq2"}, or a
user function following the \link{de_backends} contract.}

\item{model_type}{One of \code{"auto"}, \code{"spline"}, \code{"linear"} — see
\code{\link[=fit_deg_model]{fit_deg_model()}}.}

\item{pooling}{One of \code{"auto"} (default; run \code{\link[=test_pooling]{test_pooling()}} and use its
recommendation), or force \code{"pooled"}, \code{"shared_shape"}, \code{"stratified"}.}

\item{adaptive_alpha}{If \code{TRUE} (default), calibrate alpha(n) per cell
type via \code{\link[=calibrate_alpha]{calibrate_alpha()}} (empirical FDR calibration against
label-permuted null runs) and use it in place of \code{alpha} when counting
DEGs for the model. Requires \code{k_permute > 0}.}

\item{alpha}{Nominal significance threshold. Used directly if
\code{adaptive_alpha = FALSE}; used as \code{target_fpr} for \code{\link[=calibrate_alpha]{calibrate_alpha()}}
otherwise.}

\item{reference_n}{Cell count at which burden is predicted. Default
\code{NULL} uses the median, across cell types, of each cell type's full
per-group cell count (\code{min(table(group))}) — i.e. "a typical cell
type's cell count in this dataset." Pass a number to override.}

\item{min_concordance}{Downsampled replicates with a \code{concordance_metric}
value below this are excluded from model fitting entirely (\code{NULL}
disables hard exclusion; see \code{concordance_metric} and \code{weight_floor}
for soft down-weighting instead/in addition).}

\item{concordance_metric}{Which \code{\link[=concordance]{concordance()}} measure to use for
\code{min_concordance} and for weighting. Default \code{"cat_overlap"} (alpha-free).}

\item{weight_floor}{Minimum weight assigned to any retained replicate
(prevents near-zero concordance from making a point contribute
~nothing while not being hard-excluded). Default 0.1.}

\item{sig_level}{Significance threshold for the nonlinearity and pooling
LRTs.}

\item{grid_args, k, k_permute, seed}{Passed to \code{\link[=run_downsampling]{run_downsampling()}}.}

\item{...}{Passed to \code{de_fun}.}
}
\value{
A \code{burden_result} object (list) with:
\item{burden}{\code{data.frame(cell_type, n_full, reference_n, burden, burden_se, n_degs_observed)} — the main output.}
\item{downsampling}{Per-cell-type \code{\link[=run_downsampling]{run_downsampling()}} results.}
\item{concordance}{Per-cell-type \code{\link[=concordance_curve]{concordance_curve()}} results.}
\item{alpha_curves}{Per-cell-type \code{\link[=calibrate_alpha]{calibrate_alpha()}} results, or
\code{NULL} if \code{adaptive_alpha = FALSE}.}
\item{pooling_test}{\code{\link[=test_pooling]{test_pooling()}} output.}
\item{model}{The fitted model object(s) actually used for prediction.}
}
\description{
End-to-end pipeline: downsamples each cell type across a grid of cell
counts, runs DE at each, computes concordance against the full-data DE
result, optionally calibrates a per-n adaptive alpha from label-permuted
null runs, tests whether a spline is needed and whether cell types share
a common DEGs~n curve, fits the resulting model(s), and predicts the
expected DEG count ("burden") for every cell type at a common reference
cell count. See \code{docs/DESIGN.md} for the full methodology and rationale.
}
