% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_fit.R
\name{test_nonlinearity}
\alias{test_nonlinearity}
\title{Test whether a spline term is needed for DEGs ~ n}
\usage{
test_nonlinearity(
  n,
  degs,
  weights = NULL,
  cell_type = NULL,
  family = .default_family(),
  sig_level = 0.05
)
}
\arguments{
\item{n}{Numeric vector of cell counts.}

\item{degs}{Integer vector of DEG counts, same length as \code{n}.}

\item{weights}{Optional prior weights (e.g. concordance-derived), same
length as \code{n}.}

\item{cell_type}{Optional factor/character vector, same length as \code{n}.
When supplied, both the linear and spline models include \code{+ cell_type}
as an additive term (shared curve shape, per-cell-type intercept) — use
this to run the nonlinearity test on a \code{shared_shape} pooled fit rather
than a single cell type.}

\item{family}{Response family for both \code{mgcv::gam} fits. Default
\code{\link[=.default_family]{.default_family()}}, quasi-Poisson on the identity link — see that
function's docs for why.}

\item{sig_level}{Significance threshold for the nonlinearity call.}
}
\value{
\code{list(p_value, decision, model_linear, model_spline)}, \code{decision}
one of \code{"spline"} / \code{"linear"}.
}
\description{
Fits a linear negative-binomial GAM (\code{degs ~ n}) and a spline
negative-binomial GAM (\code{degs ~ s(n)}), both via \code{mgcv::gam}, and compares
them with a likelihood ratio test. A significant result (by default
p < 0.05) indicates the relationship between cell count and DEG count is
not well captured by a straight line on the model's link scale, and a
spline should be used.
}
