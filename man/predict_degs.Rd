% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_fit.R
\name{predict_degs}
\alias{predict_degs}
\title{Predict expected DEG count at a given cell count}
\usage{
predict_degs(fit, n_new, cell_type_new = NULL, se = FALSE)
}
\arguments{
\item{fit}{A \code{burden_model} from \code{\link[=fit_deg_model]{fit_deg_model()}} (or a fitted
\code{mgcv::gam} directly).}

\item{n_new}{Cell count(s) to predict at.}

\item{cell_type_new}{Optional cell type label(s) to predict at (required,
recycled to \code{length(n_new)}, if \code{fit}'s model includes a \code{cell_type}
term, e.g. a \code{shared_shape} pooled fit).}

\item{se}{If \code{TRUE}, also return the standard error of the prediction
(on the response scale, via the delta method).}
}
\value{
Numeric vector of predicted DEG counts, or if \code{se = TRUE}, a
\code{data.frame(n, fit, se)}.
}
\description{
Predict expected DEG count at a given cell count
}
