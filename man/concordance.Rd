% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concordance.R
\name{concordance}
\alias{concordance}
\title{Concordance between a downsampled DE result and the full-data DE result}
\usage{
concordance(de_res, full_res, alpha = 0.05, cat_k = NULL)
}
\arguments{
\item{de_res}{\code{data.frame} DE result for the downsampled run.}

\item{full_res}{\code{data.frame} DE result for the full-data run (reference).}

\item{alpha}{Significance threshold for the set-based measures.}

\item{cat_k}{Number of top genes to use for concordance-at-the-top.
Defaults to \code{min(50, floor(n_genes / 4))}.}
}
\value{
A one-row \code{data.frame(jaccard, f1, rank_cor, cat_overlap)}.
}
\description{
Computes three complementary concordance measures between a DE result
table from a downsampled run and the reference (full-data) DE result for
the same cell type, restricted to genes present in both tables:
\describe{
\item{jaccard}{Jaccard index of DEG sets at \code{alpha} (set overlap /
union). \code{NaN} if both sets are empty.}
\item{f1}{F1 score of the downsampled DEG set against the full DEG set
treated as truth. \code{NaN} if both sets are empty.}
\item{rank_cor}{Spearman correlation of \code{-log10(pval)} between the two
runs — alpha-free, sensitive to overall rank agreement.}
\item{cat_overlap}{Concordance-at-the-top: fraction overlap of the
top-\code{cat_k} genes by \code{abs(logFC)} in each run (alpha-free; \code{cat_k}
defaults to the smaller of 50 or a quarter of genes tested).}
}
}
