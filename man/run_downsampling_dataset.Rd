% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/downsample.R
\name{run_downsampling_dataset}
\alias{run_downsampling_dataset}
\title{Run the downsampling loop across all cell types in a dataset}
\usage{
run_downsampling_dataset(data, de_fun, ...)
}
\arguments{
\item{data}{Named list, one element per cell type, each an unnamed
\verb{list(counts = <genes x cells matrix>, group = <two-level vector>)}.
Names of \code{data} are used as cell type labels throughout the package.}

\item{de_fun}{DE backend, see \code{\link[=resolve_de_fun]{resolve_de_fun()}}.}

\item{...}{Passed to \code{\link[=run_downsampling]{run_downsampling()}} (applied identically to every
cell type; pass per-cell-type grids via \code{grid} if needed by calling
\code{\link[=run_downsampling]{run_downsampling()}} directly instead).}
}
\value{
Named list (by cell type) of \code{\link[=run_downsampling]{run_downsampling()}} results.
}
\description{
Run the downsampling loop across all cell types in a dataset
}
