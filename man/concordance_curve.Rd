% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concordance.R
\name{concordance_curve}
\alias{concordance_curve}
\title{Concordance curve across all downsampled replicates for a cell type}
\usage{
concordance_curve(downsampling_result, alpha = 0.05, cat_k = NULL)
}
\arguments{
\item{downsampling_result}{A single cell type's result from
\code{\link[=run_downsampling]{run_downsampling()}} (i.e. \code{run_downsampling_dataset(...)[[cell_type]]}).}

\item{alpha, cat_k}{Passed to \code{\link[=concordance]{concordance()}}.}
}
\value{
\code{data.frame(n, rep, jaccard, f1, rank_cor, cat_overlap)}.
}
\description{
Applies \code{\link[=concordance]{concordance()}} to every replicate in a \code{\link[=run_downsampling]{run_downsampling()}}
result, against that cell type's full-data result, and returns one row
per (n, replicate) with \code{n}, \code{rep}, and the concordance measures. Intended
to be inspected/plotted as a function of \code{n}, and optionally used to
derive weights or an exclusion cutoff before fitting the DEG~n model (see
\code{min_concordance}/\code{concordance_weight} in \code{\link[=fit_deg_model]{fit_deg_model()}}).
}
