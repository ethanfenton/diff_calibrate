% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_fit.R
\name{test_pooling}
\alias{test_pooling}
\title{Test whether a pooled (across cell type) model is justified}
\usage{
test_pooling(
  n,
  degs,
  cell_type,
  weights = NULL,
  family = .default_family(),
  sig_level = 0.05
)
}
\arguments{
\item{n, degs, cell_type}{Equal-length vectors pooling downsampling results
across cell types.}

\item{weights}{Optional prior weights.}

\item{family}{Response family for all three \code{mgcv::gam} fits. Default
\code{\link[=.default_family]{.default_family()}}.}

\item{sig_level}{Significance threshold for both LRTs.}
}
\value{
\code{list(recommendation, offset_test, shape_test, model_pooled, model_shared_shape, model_stratified)}. \code{recommendation} is one of
\code{"pooled"}, \code{"shared_shape"}, \code{"stratified"}.
}
\description{
Compares three nested \code{mgcv::gam} fits of DEGs ~ n across all cell types
in a combined dataset:
\itemize{
\item \code{pooled}: \code{degs ~ s(n)} — single shared curve, no cell-type effect.
\item \code{shared_shape}: \code{degs ~ s(n) + cell_type} — shared curve shape,
per-cell-type intercept (random-intercept-like via a fixed factor).
\item \code{stratified}: \code{degs ~ s(n, by = cell_type) + cell_type} — fully
separate curve per cell type.
}
Two LRTs are run: \code{pooled} vs \code{shared_shape} (is a per-cell-type offset
needed at all?) and \code{shared_shape} vs \code{stratified} (is a per-cell-type
curve \emph{shape} needed, beyond an offset?). Use the result to decide which
of the three to actually use for prediction (see \code{recommendation}).
}
