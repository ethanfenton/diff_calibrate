% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_fit.R
\name{lrt_test}
\alias{lrt_test}
\title{Nested-model likelihood ratio test between two \code{mgcv::gam} fits}
\usage{
lrt_test(model_null, model_alt, min_df = 0.4)
}
\arguments{
\item{model_null, model_alt}{\code{mgcv::gam} objects, \code{model_null} nested
within \code{model_alt}.}

\item{min_df}{Minimum extra (effective) degrees of freedom \code{model_alt}
must use beyond \code{model_null} for the test to be considered meaningful.
When a penalized smooth collapses back onto (or very near) its nested
null — e.g. a spline that shrinks to an effectively straight line — the
nominal F/chi-square test can become numerically degenerate (near-zero
denominator df) and report spuriously tiny p-values despite the two
models being practically identical. Below this threshold the test is
short-circuited to \code{p_value = 1} rather than trusting that degenerate
statistic. Default 0.4.}
}
\value{
A one-row \code{data.frame(p_value, chisq, df)}.
}
\description{
Thin wrapper around \code{anova.gam(..., test = "Chisq")} used for both the
spline-need test (\code{\link[=test_nonlinearity]{test_nonlinearity()}}) and the pooling-need test
(\code{\link[=test_pooling]{test_pooling()}}) — both reduce to "is a more flexible nested model
justified over a simpler one," so they share this helper.
}
\keyword{internal}
