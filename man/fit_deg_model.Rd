% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_fit.R
\name{fit_deg_model}
\alias{fit_deg_model}
\title{Fit the DEGs ~ n model for a single cell type}
\usage{
fit_deg_model(
  n,
  degs,
  weights = NULL,
  cell_type = NULL,
  model_type = c("auto", "spline", "linear"),
  family = .default_family(),
  sig_level = 0.05
)
}
\arguments{
\item{n}{Numeric vector of cell counts (one per downsampling replicate).}

\item{degs}{Integer vector of DEG counts, same length as \code{n}.}

\item{weights}{Optional prior weights, e.g. derived from
\code{\link[=concordance_curve]{concordance_curve()}} (see \code{min_concordance}/\code{weight_from}).}

\item{cell_type}{Optional cell-type vector, see \code{\link[=test_nonlinearity]{test_nonlinearity()}};
used for \code{shared_shape} pooled fits.}

\item{model_type}{One of \code{"auto"} (default; run \code{\link[=test_nonlinearity]{test_nonlinearity()}} and
pick), \code{"spline"} (force spline), \code{"linear"} (force linear).}

\item{family}{Passed to \code{\link[=test_nonlinearity]{test_nonlinearity()}}.}

\item{sig_level}{Significance threshold used when \code{model_type = "auto"}.}
}
\value{
A \code{burden_model} object: \code{list(model, model_type, decision, nonlinearity_test)}, where \code{nonlinearity_test} is the full
\code{\link[=test_nonlinearity]{test_nonlinearity()}} output (always computed, even when \code{model_type}
is forced, so the decision that \emph{would} have been made is visible).
}
\description{
Fit the DEGs ~ n model for a single cell type
}
