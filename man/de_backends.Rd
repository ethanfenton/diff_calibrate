% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de_wrappers.R
\name{de_backends}
\alias{de_backends}
\title{Built-in and pluggable differential expression backends}
\description{
A DE backend is any function with signature \verb{function(counts, group, ...)}
returning a \code{data.frame} with columns \code{gene}, \code{pval}, \code{padj}, \code{logFC}.
\code{counts} is a genes x cells numeric matrix (raw counts, not normalized,
unless the backend expects otherwise), \code{group} is a factor/character vector
of length \code{ncol(counts)} giving the two-level grouping to test. Any
function following this contract can be passed as \code{de_fun} to
\code{\link[=calculate_burden]{calculate_burden()}} or \code{\link[=run_downsampling]{run_downsampling()}} directly; the wrappers below
are convenience implementations for common methods.
}
