% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/downsample.R
\name{downsample_counts}
\alias{downsample_counts}
\title{Downsample a counts matrix to a target number of cells per group}
\usage{
downsample_counts(counts, group, n_per_group, seed = NULL)
}
\arguments{
\item{counts}{Genes x cells matrix.}

\item{group}{Length-\code{ncol(counts)} grouping vector.}

\item{n_per_group}{Target cell count per group level.}

\item{seed}{Optional integer seed for reproducible sampling.}
}
\value{
\verb{list(counts = <subsetted matrix>, group = <subsetted group>)}.
}
\description{
Samples \code{n_per_group} cells (columns) without replacement from each level
of \code{group} independently. If a group has fewer than \code{n_per_group} cells,
all of its cells are kept (with a warning) rather than erroring, since
upstream grid construction should generally prevent this.
}
