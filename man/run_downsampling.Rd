% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/downsample.R
\name{run_downsampling}
\alias{run_downsampling}
\title{Run the downsampling + DE loop for a single cell type}
\usage{
run_downsampling(
  counts,
  group,
  de_fun,
  grid = NULL,
  grid_args = list(),
  k = 5,
  k_permute = 5,
  alpha = 0.05,
  seed = 1,
  ...
)
}
\arguments{
\item{counts}{Genes x cells matrix for this cell type.}

\item{group}{Two-level grouping vector (e.g. treatment) for this cell type.}

\item{de_fun}{DE backend: string name or function, see \code{\link[=resolve_de_fun]{resolve_de_fun()}}.}

\item{grid}{Integer vector of per-group cell counts; if \code{NULL}, built via
\code{\link[=downsample_grid]{downsample_grid()}} using \code{grid_args}.}

\item{grid_args}{List of extra arguments to \code{\link[=downsample_grid]{downsample_grid()}} (\code{n_min},
\code{n_points}) when \code{grid} is not supplied directly.}

\item{k}{Number of downsampling replicates per grid point.}

\item{k_permute}{Number of label-permuted replicates per grid point, for
empirical alpha calibration. Set to \code{0} to skip (saves compute if
adaptive alpha is not needed).}

\item{alpha}{Nominal significance threshold used to compute \code{n_degs} in
the returned table (in addition to the full \code{de_res} table, which lets
downstream code recompute at other thresholds without rerunning DE).}

\item{seed}{Optional integer seed; downstream draws are seeded
deterministically off of this (\code{seed + rep_index}) for reproducibility.}

\item{...}{Passed to \code{de_fun}.}
}
\value{
A list with:
\item{full}{\code{data.frame} DE result at full cell count (the reference).}
\item{downsampled}{\code{data.frame} with one row per (n, replicate):
\code{n}, \code{rep}, \code{n_degs}, plus a list-column \code{de_res} holding the full DE
table for that replicate (used by \code{\link[=concordance]{concordance()}}).}
\item{permuted}{Same shape as \code{downsampled}, from label-shuffled runs,
or \code{NULL} if \code{k_permute == 0}.}
}
\description{
For a grid of per-group cell counts, draws \code{k} replicate downsamples at
each grid point and runs \code{de_fun} on each, plus (optionally) \code{k_permute}
label-permuted replicates per grid point for empirical null calibration
(see \code{\link[=calibrate_alpha]{calibrate_alpha()}}). Always also runs DE once on the full data as
the reference/ground-truth result.
}
